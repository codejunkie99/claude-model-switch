---
name: cms-start
description: Start the model switch proxy
---

# Start Proxy

Start the claude-model-switch proxy server.

## Steps

1. Run `claude-model-switch start` and show the output.
2. Confirm the proxy is running with `claude-model-switch status`.
